<nav class="font-sans flex flex-col text-center sm:flex-row sm:text-left sm:justify-between py-4 px-6 bg-white shadow sm:items-baseline w-full">
  <div class="mb-2 sm:mb-0">
    <a href="/" class="text-2xl no-underline text-grey-darkest hover:text-blue-dark">Home</a>
  </div>
  <div class="inline-block">
    <% if @current_user %>
      Signed as <%= Current.user.email %>
      <%= link_to "Log out", Current.session, method: :delete, data: { "turbo-method": "DELETE" }, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2" %>
    <% else %>
      <%= link_to "Sign In", sign_in_path, class: "text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2" %>
    <% end %>
  </div>
</nav>
